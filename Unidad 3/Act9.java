import java.lang.Math;

public class Act9{
    public static void main (String [] args){
        int x,y,suma, resta, mult;
        double div;
        x=144;
        y=999;
        suma= (x+y);
        resta=(x-y);
        mult=(x*y);
        div= (double)x/y;
        System.out.println("X: "+ x + "\nY: " + y + "\n\nSuma: " + suma + "\nResta: " + resta + "\nDivisión: " + div + "\nMultiplicación: " + mult);
    }
}
